import React from 'react'
import styles from './HomePage.scss'
import Button from 'components/atoms/Button'

const HomePage = ({}) => (
  <div className="Home">Home Page<Button /></div>
);

HomePage.defaultProps = {}

export default HomePage
